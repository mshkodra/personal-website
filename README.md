Static site in `public/`, Bash API in `api/`. Don't move `public/better-radius.js` — a userscript auto-updates from that URL.

New post? Add its slug to the allowlist in `api/vote.sh`, or its votes 404.
